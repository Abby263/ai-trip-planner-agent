# Intent Parser Prompt

Extract structured travel intent from the user request. Return only validated JSON matching the intent parser schema. Prefer assumptions only when the plan can proceed safely.
